<h1>Sign In</h1>
<%= bootstrap_form_for(@admin, :url => {:action => 'do_log_in'}, :method => 'post') do |f| %>
    <%= f.email_field :email %>
    <%= f.password_field :password %>
    <%= f.submit "Sign in", class: "btn btn-success" %>
<% end %>

